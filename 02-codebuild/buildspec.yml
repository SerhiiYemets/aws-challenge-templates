version: 0.2

phases:
  install:
    commands:
      - echo "Install phase"

  pre_build:
    commands:
      - echo "Pre-build phase"

  build:
    commands:
      - echo "Build phase"
      - ls -la
      - grep "AWS CodeBuild works!" index.html

  post_build:
    commands:
      - echo "Build completed successfully"

artifacts:
  files:
    - index.html
