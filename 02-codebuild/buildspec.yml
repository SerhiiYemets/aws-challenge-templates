version: 0.2

phases:
  install:
    commands:
      - echo "Install phase"

  pre_build:
    commands:
      - echo "Pre-build phase"

  build:
    commands:
      - echo "Build phase"
      - ls -la 02-codebuild
      - grep "AWS CodeBuild works!" 02-codebuild/index.html

  post_build:
    commands:
      - echo "Build completed successfully"

artifacts:
  files:
    - 02-codebuild/index.html
